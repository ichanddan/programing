// sign of arrow function is => 
const arrowsum= (a,b)=>{
    console.log(a+b)
}
arrowsum(5,5);

//  mutiply two valu using arrow function
const arrowmul= (a,b)=>{
    console.log(a*b)
}
arrowmul(5,5);
const arrowmu1= (a,b)=>{
    return a *b
}
arrowmul(5,5);

// the below code fragment can be found in: