// looping over an array 
let arrays=["chandan",'aman','nitu', 'kingsman'];
for(let name of arrays){
    console.log(name.toLocaleUpperCase());
    console.log(name.length);
    console.log(typeof name);
}